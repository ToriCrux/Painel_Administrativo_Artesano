spring:
  datasource:
    url: jdbc:h2:file:./data/ladrilhos;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH;AUTO_SERVER=TRUE
    driverClassName: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate.format_sql: true
      hibernate.jdbc.time_zone: UTC

  h2:
    console:
      enabled: true
      path: /h2-console

  flyway:
    locations: classpath:db/migration
    baseline-on-migrate: true

server:
  error.include-message: always

springdoc:
  swagger-ui:
    path: /swagger-ui.html

jwt:
  secret: 'kKm5G2MIj9aMOK/y6v0tXpLHmv8nciqTmrLb+HlJk3k='
  expiration: 28800

